# web

React and TypeScript frontend for Mills-Operation (Vite, Tailwind CSS, Recharts).

See the repo root [`README.md`](../README.md) for how to run the whole application. This folder
only handles the frontend: `npm install`, then `npm run dev` (with the backend already running).
